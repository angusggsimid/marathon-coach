# Marathon 下一版本方向：可信调整与执行闭环

日期：2026-07-15
状态：已确认方向，作为本轮唯一实施依据

## 1. 版本目标

这一版本不扩展新的产品主线，只加深现有核心价值：

> 用户完成训练并打卡后，系统能够清楚解释为什么调整下周计划；用户理解调整后，可以把最新计划正确导出或同步到执行设备。

完整闭环：

`训练打卡 → 周度总结 → 计划调整 → 解释原因 → 导出/同步最新计划 → 继续训练`

## 2. 用户问题

当前产品已经能够根据上周完成率和 RPE 调整下一周训练距离，但仍有四个问题：

1. 用户看到 `±N%`，不一定理解为什么变化，也不知道哪些内容没有变化。
2. 打卡数据只影响算法，缺少一个清楚的周度复盘结果。
3. FIT 默认导出整份计划，第一次使用和频繁更新时负担过大。
4. 计划变化后，用户可能继续执行手表、日历或 Intervals.icu 中的旧计划。

## 3. 本轮范围

本轮只实现以下四项，并把它们组合成两组体验。

### 3.1 本周状态

#### A. 调整三行证明

当本周训练距离发生自动调整时，在本周视图顶部展示固定三行：

1. **改了什么**：`本周训练距离已自动减少 10%`。
2. **依据什么**：`上周打卡 3/5，完成率 60%，平均体感“累”`。
3. **什么没变**：`配速、课型、比赛日和休息日不变`。

要求：

- 必须复用现有 `weekly-adaptation` 计算结果，禁止在 UI 再写一套判断规则。
- 可展开查看上一完整周日期范围、打卡数、完成率、平均 RPE 和本周作用范围。
- 没有足够打卡时不显示调整证明，不得假装完成评估。
- `factor = 1` 不显示强调卡，结果放入周报弱提示，避免界面拥挤。
- 不改算法阈值，不增加 AI 文案，不允许用户直接手调系数。

#### B. 轻量周报

在本周视图提供“上周 60 秒小结”，内容控制在五项以内：

- 打卡节数与完成率
- 计划距离与完成情况的可用估算
- 平均 RPE
- 对本周计划的影响
- 本周最关键的一节训练

要求：

- 与三行证明共享同一个周快照和口径，不重复计算。
- 没有上周打卡时展示诚实空态和“去补记”入口，不显示零分报表。
- 周日和周一提高视觉权重，其余时间仍可查看但不弹窗。
- 支持复制周报；剪贴板失败时提供明确失败提示。
- 不做邮件、系统通知、年度报告、排行榜或社交 Feed。

### 3.2 导出与同步

#### C. FIT 范围导出

在现有 Garmin FIT 导出流程中增加三个范围：

- 今天
- 本周
- 全部计划

要求：

- 必须使用 `effectivePlan`，确保包含赛事覆盖、休假和周自适应后的最新结果。
- 选择项旁显示实际文件数量。
- 今天或本周没有可导出的跑步训练时禁用对应选项，并说明原因。
- ZIP 文件名包含范围和日期。
- 不做任意日期范围、不接 Garmin/COROS 私有云 API。

#### D. 计划版本与重新处理提醒

当用户曾经导出或同步过计划，之后 `effectivePlan` 内容发生变化时，显示非阻塞提醒：

> 训练计划已更新，你之前导出或同步的版本可能已过期。

要求：

- 用稳定的计划指纹判断是否变化，指纹基于排序后的训练日期、类型、距离和描述。
- 仅当用户曾成功执行对应操作后才显示提醒；从未导出或同步的用户不提示。
- FIT、ICS、Intervals.icu 分渠道记录最后成功时间、范围和计划指纹。
- FIT/ICS 重新导出成功后，只清除对应渠道的提醒。
- Intervals.icu 不得后台自动同步。
- **幂等安全门槛**：如果当前 Intervals.icu 实现无法证明重复同步不会创建重复事件，就不得提供“一键重新同步”并宣称安全。必须先实现并测试稳定 `external_id`/upsert，或保留明确的人工清理警告。
- API Key 继续只保存在当前会话，不得重新持久化。

## 4. 统一信息架构

### 4.1 本周视图

顺序固定为：

1. 本周标题与比赛倒计时
2. 本周状态区
   - 有调整：三行证明
   - 上周小结：折叠内容或次级区域
3. 计划版本过期提醒（仅需要时）
4. 每日训练列表

不得把三行证明、周报和同步提醒做成三个同等重量的大卡片。

### 4.2 导出 Sheet

沿用现有导出入口，不新增独立页面：

- Garmin FIT：今天 / 本周 / 全部
- 日历 ICS：保持现有入口，记录成功版本
- Intervals.icu：保持设置和同步入口；只有满足幂等安全门槛后才允许明确的重新同步 CTA

## 5. 技术边界

### 5.1 共享周快照

新增一个纯函数模块，统一输出：

- 上一完整周起止日期
- 计划训练数
- 打卡数
- 完成率
- 平均 RPE
- 计划距离与可用完成距离估算
- 当前自适应系数和原因
- 本周关键训练

三行证明和周报只能消费这一份结果。

### 5.2 计划指纹

新增纯函数生成稳定指纹：

- 过滤 `Rest`
- 日期标准化为本地 `YYYY-MM-DD`
- 按日期与训练标识稳定排序
- 只包含会影响执行的字段
- 不使用随机值、当前时间、对象序列化顺序或 UI 文案状态

持久化结构需要版本迁移，旧用户必须安全回退为“尚未记录导出版本”，不能出现错误提醒。

### 5.3 本地优先和隐私

- 所有周报、计划指纹和导出记录保存在本地。
- 不新增账号、云数据库、分析 SDK 或用户行为上传。
- 不记录或输出 Intervals.icu API Key。

## 6. UI 与交互要求

- 沿用现有训练页、卡片、按钮和语义色，不创建新的设计语言。
- 减量使用橙色语义，加量使用现有强调色，保持状态使用中性弱样式。
- 所有颜色信息必须同时有文字或图标，不能只靠颜色表达。
- 手机宽度下三行证明、周报和提醒不能横向溢出。
- 可展开区域使用明确按钮和 `aria-expanded`。
- 导出范围使用分段选择或菜单项，显示图标、范围和文件数量。
- 非阻塞提醒不得覆盖训练列表或底部导航。
- 不使用弹窗强迫用户阅读周报。

## 7. 明确不做

- 不修改训练自适应阈值或训练计划算法。
- 不做 Momentum、徽章、排行榜或 streak 改造。
- 不做赛事变更单和字段冲突解释。
- 不做通用 AI 教练、伤病判断、社区或年度赛季编排。
- 不做后台自动同步和私有 API 逆向。
- 不为了提醒而制造新的通知权限流程。

## 8. 验收标准

### 8.1 自动测试

- 周快照：无打卡、低完成高 RPE、正常保持、高完成低 RPE、部分完成、跨周日期。
- 三行证明：系数和文案与现有算法一致。
- FIT 范围：今天、本周、全部、Rest 空范围、本地日期边界。
- 计划指纹：稳定性、字段变化触发、无关 UI 状态不触发、旧持久化迁移。
- 各渠道成功操作只更新对应同步元数据。
- Intervals.icu 幂等同步若进入本轮，必须有重复调用不产生重复事件的合同测试。

### 8.2 浏览器验收

桌面和手机都要覆盖：

1. 无打卡时的诚实空态。
2. 减量 10% 的三行证明和展开证据。
3. 保持计划时的轻量周报。
4. 今天无训练时 FIT 选项禁用。
5. 本周 FIT 文件数量和下载。
6. 计划变化后出现过期提醒。
7. 对应渠道重新导出后提醒正确清除。
8. 页面无横向溢出、遮挡、控制台错误和布局跳动。

### 8.3 工程门禁

- `npm run lint`
- `npm run build`
- `npm run test:core`
- crawler TypeScript 和赛事标准化测试不得回归
- 不降低现有 40 项浏览器主路径验收结果

## 9. 完成定义

只有同时满足以下条件才算完成：

1. 四项范围全部进入真实用户流程，不是孤立工具函数。
2. 三行证明与周报共享同一数据口径。
3. FIT 范围导出使用最新 `effectivePlan`。
4. 计划变化提醒不会对从未导出/同步的用户误报。
5. Intervals.icu 不会因重新同步提醒造成静默重复事件；无法证明时必须诚实限制。
6. 自动测试、生产构建和桌面/手机浏览器验收全部通过。
7. `CONTEXT.md` 记录实际完成内容、验证结果和遗留风险。

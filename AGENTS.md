# AGENTS.md

本项目默认遵守全局规则：

`/Users/agg/.codex/AGENTS.md`

项目内如有特殊约定，写在本文件下方；未特别说明的事项一律按全局规则执行。

## 本项目补充规则

## 默认协作模型：gonly

本项目默认使用 **gonly**（仅 Grok 协作，不调用 Kimi）。

适用默认：

1. 工程实现、重构、修 bug、加测试、改 CI/部署配置。
2. 代码审查、诊断、失败模式分析、架构取舍。
3. 需要读改工作区、跑 Shell/测试、写文档的任务。

可跳过 gonly 的例外：

1. 纯问答、概念解释、与仓库无关的讨论。
2. 极小只读检查（例如只确认某个文件是否存在、某行文案是什么），且不修改任何文件。

硬性约束：

1. 除上述例外外，工程实现/审查/诊断默认先调用 gonly。
2. Grok 出现认证、权限、限流、网络、超时、服务端或模型不可用等故障时，**按 gonly skill 立即停止**，向用户报告原始错误与已产生修改；**绝不切 Kimi**，不得静默降级或自动换模型继续。
3. 除非用户当轮明确授权，不执行 Git 提交/推送、部署、删除、不可逆迁移或外部提交。

## 数据决策系统优先级

当任务涉及 tracker、monitor、dashboard、决策体系、投资研究系统时，必须先完成数据和判断，再做产品封装。

执行顺序固定为：

1. 明确要帮助用户判断什么。
2. 建立真实数据底表：数值、日期、来源、单位、频率、口径、解释力。
3. 定义数据如何 track、如何去重、哪些剔除。
4. 建立决策规则和反证条件。
5. 输出最小可用判断。
6. 最后才做 UI、dashboard 或产品封装。

硬性约束：

1. 没有真实数据底表时，不允许交付产品壳。
2. 没有最小可用判断时，不允许声称 tracker 完成。
3. “显示数据缺失”只能算风险暴露，不能算完成。
4. 验收必须包含“是否对用户决策有用”，不能只看构建通过、页面可访问、控制台无错误。

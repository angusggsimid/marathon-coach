# COROS MCP Deep Research：手表闭环与产品进化方向

> 日期：2026-08-15
> 性质：调研 + 产品构思（未改任何产品代码；结论需用户决策后才进实施）
> 视角：马拉松备赛产品经理

---

## 0. 一页结论

1. **COROS 是第一个发布官方 MCP 的主流耐力表品牌**（2026-05-19 官宣，与 PACE 4 Ingebrigtsen 版同周）。端点 `https://mcp.coros.com/mcp`，远程托管，用户用 COROS 账号授权。
2. **现在只读，但官方承诺 2026 年 9 月前开放写入**（AI 直接把训练计划/课表写进 COROS App）。这个时间点落在我们的 90 天 PWA 路线内，必须提前卡位。
3. 对我们的意义不是"接个 API"，而是**三个方向的闭环机会**：
   - **读**：拿真实训练数据（HRV/睡眠/实际配速/阈值）替代主观打卡，让自适应从"信任用户"升级为"信任身体"；
   - **写**：9 月后计划直推手表，干掉 FIT 文件手动导入的最后一公里；
   - **反向**：把我们的计划引擎做成 MCP Server，让 Claude/ChatGPT 用户直接调用我们的计划——这是 AI 时代的新分发渠道。
4. **最大矛盾**：官方 MCP 目前要求 Claude Pro / ChatGPT Plus 付费订阅，而我们的主体用户是中国跑者——短期内"用户自己连 MCP"是小众玩法。**真正的机会是我们替用户连**（产品内嵌），或先做不依赖 MCP 的低风险铺垫（LT 校准、手动导入闭环）。
5. **建议**：不赌单一方向。90 天内做三件可逆的小事（见 §6），同时盯两个外部触发器（COROS 写入 API 上线、Garmin 是否跟进 MCP）。

---

## 1. 事实基础（Evidence）

### 1.1 官方 COROS MCP（来源：support.coros.com 官方指南，2026-07-02 更新）

| 维度 | 事实 |
|---|---|
| 端点 | `https://mcp.coros.com/mcp`（远程 MCP，COROS 账号授权） |
| 时间线 | 2026-05-19 官宣；2026-06 更新扩展数据面；**2026-09 前计划开放写入** |
| 支持平台 | Claude Pro（推荐）、ChatGPT Plus（需 Developer Mode，会关记忆）、Cursor、Gemini CLI |
| 权限粒度 | 用户可在 Claude 侧按工具级授权（全开/需批准/禁用） |
| 隐私边界 | 只能读自己的数据；教练/运动员可见性规则与 Training Hub 一致 |

**可读数据面（2026-06 更新后）**：

- 活动：活动记录、分圈/分段、训练反馈与笔记、**.FIT 原始文件含 GPS（每日 50 个文件上限）**
- 健康：睡眠、每日心率、HRV、压力、Wellness Checks、静息心率、经期、步数/卡路里
- 体能：**VO2 Max、Running Fitness、比赛预测成绩、恢复状态、乳酸阈配速、配速区间**、周训练负荷
- 计划：**当前训练计划与训练日历**
- 账号/设备：用户档案、已连接设备

**官方 FAQ 关键问答**：
- 「能否让 AI 写回训练计划？」→ 「目前只读。**我们计划在 2026 年 9 月前发布更新，允许通过 AI 直接把训练计划和课表写入 COROS App**。」
- FIT 深度分析（GPS 路线、逐分段对比、骑行功率）必须走 .FIT 文件请求，50 个/天。

### 1.2 社区生态（验证了需求与技术路径）

| 项目 | 能力 | 意义 |
|---|---|---|
| `cygnusb/coros-mcp` | 只读：睡眠/HRV/训练，Training Hub 凭据直连 | 官方 MCP 之前就有需求 |
| `dholliday3/coros-training-mcp`（PyPI 可装） | **已实现写入**：创建/编辑/排期跑步课表（配速区间、重复组、力量课），走非官方 `teamapi.coros.com`（含 CN/EU/Global 分区） | 证明「课表直推手表」工作流成立；其工具 schema（`create_run_workout` / `schedule_workout` / `move_scheduled_workout`）可直接作为我们未来对接的参考契约 |

### 1.3 竞争格局

- 截至本次调研，**Garmin / Polar / Suunto 均无官方 MCP**（the5krunner、technologiainfotech 报道口径一致：COROS 是第一个）。
- COROS 的动作配合 PACE 4 发布，明显把「AI + 训练数据」当作差异化卖点在经营。
- 推论：其他品牌跟进是时间问题（Garmin Connect API 生态更成熟，一旦出 MCP 影响更大）。我们的设计必须**品牌中立**，COROS 是首发场景而非唯一场景。

### 1.4 我们产品的现状对照

| 现有能力 | 与 COROS 的关系 |
|---|---|
| 训练引擎配速模型 | 本来就是 **COROS EvoLab 6 区间**模型——品牌语言天然对齐 |
| FIT 导出（今天/本周/全部） | 单向推给 Garmin Connect；COROS 用户目前无官方导入通路 |
| Intervals.icu 同步 | 间接覆盖 COROS（ICU 可同步到 COROS），但幂等未证明、依赖用户自己的 ICU 账号 |
| 打卡/自适应 | 完全靠用户主观申报（完成状态 + RPE） |
| 赛事库 | 1253 场中国赛事 + 多源确认——**这是 COROS/任何手表厂商都没有的资产** |

---

## 2. 产品经理视角：这件事改变了什么

### 2.1 范式转变

过去：`我们生成计划 → 导出文件 → 用户手动搬运 → 手表执行 → 数据留在手表里 → 我们看不见`

MCP 之后可能变成：`我们生成计划 → 直推手表 → 手表记录真实执行 → 我们读回身体数据 → 计划自动校准`

**这是从「计划生成器」到「闭环教练」的跃迁。** 闭环的每一段都对应一类新功能。

### 2.2 我们的护城河在哪

COROS 把数据访问民主化之后，「能读到数据」不再是壁垒——任何人都能在 COROS 数据上搭 AI 教练。我们的不可替代资产是：

1. **训练引擎**（VDOT + 周期化 + 降量科学，纯函数、可测试、有理论出处）
2. **中国赛事库**（1253 场、每日刷新、多源确认——手表厂商和海外 AI 都没有）
3. **中文跑者语境**（微信生态、报名季、签运文化）

所以战略姿态应该是：**做 AI 时代最好的「计划来源 + 赛事情报」，让各种 AI/手表来调用我们**，而不是去和 COROS 抢数据分析。

---

## 3. 机会清单（发散）

### A 组：读侧闭环（MCP 读数据 → 计划更聪明）

- **A1. LT 自动校准**：连接后读取手表实测阈值配速，替换我们公式推算的 LT。计划精度一次性跃升，且只需读一个字段。**最便宜的精度红利。**
- **A2. 客观自适应**：现在的每周自适应靠打卡 + RPE（主观、经常忘打）。引入实际训练负荷、HRV、睡眠 → 自适应系数从「你说你累」变成「数据显示你累」。
- **A3. 验证打卡（Verified）**：打卡可从「申报制」变「核销制」——当天有匹配活动即自动标记完成。衍生社交价值：**「真实完成」分享卡**，跑步群里的可信度货币。
- **A4. 赛前状态问答**：COROS 有比赛预测 + 恢复状态，我们有目标赛事 + 计划进度。合并回答「我六周后比赛，现在什么状态？」——官方示例问题里原话就是这个需求。
- **A5. 赛后闭环**：从活动流识别出比赛（距离 + 强度 + 报名库匹配）→ 自动提示更新 PB → 重算 VDOT → 生成恢复期 + 下一周期。把「比完赛就流失」变成「比完赛就续命」。

### B 组：写侧直连（2026-09 官方写入后）

- **B1. 计划直推 COROS**：90 天计划一次性写成 COROS 训练计划，手表端原生执行。FIT 手动导入降级为 Garmin 用户的备选。
- **B2. 变更同步**：我们侧的降量/休假/自适应调整 → 同步修改手表端课表（对应 `update/move/replace_scheduled_workout` 语义）。**难点是幂等与冲突处理——我们在 ICU 同步上踩过的坑（部分成功、重复事件）会原样重现，必须复用 plan-fingerprint + allSucceeded 合同的纪律。**
- **B3. 双通道策略**：COROS 官方写入 + 社区 `coros-training-mcp` 的非官方写入（今天就能用，但违反 ToS 风险 + 随时失效）。**建议只走官方**；非官方路径仅用于研究 schema。

### C 组：我们作为 MCP Server（反向分发）

- **C1. Marathon MCP Server**：把计划引擎包成 MCP（工具如 `get_today_workout` / `get_week_plan` / `explain_why_this_workout` / `get_race_candidates` / `generate_plan`）。用户把 Claude 同时连上 COROS MCP + Marathon MCP，就能得到「数据 + 计划」双源 AI 教练——而我们提供的正是 COROS 给不了的那一半。
- **C2. 赛事情报 MCP**：`search_races(city, month, distance)` / `race_registration_status`。中国赛事信息是独家资产，开放给 AI 生态 = 免费获客。
- **C3. 分发逻辑**：MCP 目录 / Claude 连接器生态是新的「应用商店」。早期进入者吃心智红利。实现成本极低（本地 stdio server 包一层纯函数引擎即可，引擎本来就是无副作用纯函数）。

### D 组：体验与商业衍生

- **D1. 「AI 备赛报告」**：用读侧数据生成月度深度报告（我们出结构，AI 出叙述），可分享、可传播。
- **D2. 付费点候选**：产品目前全免费。闭环功能（连接手表、客观自适应、计划直推）是第一个用户愿意付费的候选——但先验证留存再谈收费。
- **D3. 跑团/教练版远景**：一个教练账号管多个运动员计划。注意：COROS MCP 明确不开放教练读运动员数据，此路在 COROS 侧暂时走不通，仅记为远景。

---

## 4. 风险与约束（不回避）

| # | 风险 | 影响 | 对策 |
|---|---|---|---|
| R1 | **中国用户 × 海外 AI 订阅悖论**：官方 MCP 当前必须经 Claude Pro/ChatGPT Plus 使用，主体中国用户够不着 | 「用户自连 MCP」短期是小众 | 我们替用户连（产品内嵌），或先做不依赖 MCP 的铺垫功能；等 COROS 出普通 OAuth API |
| R2 | 写入 API 是**承诺不是事实**（「计划 9 月前」） | B 组功能可能扑空 | 不把 90 天目标押在 B 组；设外部触发器，官宣后再启动 |
| R3 | 非官方 API（teamapi）违反 ToS、随时失效 | 社区方案不可作为生产依赖 | 只用于研究 schema；生产只走官方 |
| R4 | 隐私品牌冲突：我们的卖点是「数据全在本地」 | 拉取 COROS 数据改变叙事 | 可选连接 + 本地存储 + 明确授权文案；沿用备份功能的白名单纪律 |
| R5 | Garmin 用户被冷落 | 主流手表保有量 Garmin > COROS | FIT/ICS 通道永远保留；MCP 是增量不是替代 |
| R6 | FIT 50 个/天限额、MCP 服务稳定性（官方 FAQ 自认偶发错误） | 批量场景受限 | 按日限额设计同步节奏；失败重试 + 诚实报错（复用导出 sheet 的失败可见性模式） |
| R7 | 我们读到的 COROS 配速区间 vs 自算区间不一致 | 用户困惑「两个 Z2」 | 连接后以手表实测为准并明示来源；不静默覆盖 |

---

## 5. 决策分叉（给用户）

```
Q1: 是否接受「闭环教练」作为下一阶段的北极星？
    ├─ 否 → 维持现状，FIT/ICU 通道够用，本文档存档
    └─ 是 → Q2
Q2: 90 天内投入多深？
    ├─ 轻（推荐）→ 做 §6 三件小事（总计约 1-2 周工作量），等外部触发器
    ├─ 中 → 轻 + C1 Marathon MCP Server 公开发布（抢生态位）
    └─ 重 → 中 + 现在就基于非官方 API 做写入原型（不建议，R3）
Q3: 9 月 COROS 写入 API 官宣后？
    └─ 启动 B 组，复用 plan-fingerprint / allSucceeded 纪律做幂等同步
```

---

## 6. 建议的 90 天行动（轻方案，全部可逆）

| # | 事项 | 依赖 | 为什么先做 |
|---|---|---|---|
| 1 | **LT 校准（手动版）**：支持用户从 Training Hub 导出/手填实测阈值，替换推算值 | 无需任何 API | A1 的零风险铺垫，立即提升计划精度 |
| 2 | **Marathon MCP Server 原型**：本地 stdio，暴露 `get_today_workout` / `explain_plan` / `search_races` 等只读工具 | 引擎是纯函数，成本低 | C 组卡位；先自己用起来（OpenCode/Claude 直连），验证工具设计 |
| 3 | **监控两个外部触发器**：① COROS 写入 API 官宣 ② Garmin/Polar 是否发 MCP | 无 | 触发即重评 B 组与品牌优先级 |

明确后置：客观自适应（A2）、验证打卡（A3）、计划直推（B 组）、收费（D2）——全部等触发器或用户决策。

---

## 7. 来源清单

- COROS 官方支持文档《COROS MCP: A Guide to Connecting Your Training Data to AI》（2026-07-02 更新）——数据面、只读现状、9 月写入承诺、平台要求
- the5krunner《New COROS MCP: what you now hand over to AI》（2026-05-13/14）——首发报道、数据范围、「第一个主流耐力表品牌」
- trailandkale / androidauthority / technologiainfotech——发布时间线（2026-05-19 与 PACE 4 同周）
- github.com/cygnusb/coros-mcp——社区只读方案
- dholliday3.github.io/coros-training-mcp + PyPI——社区写入方案与工具 schema
- natecue.com 实践博客——用户侧真实用法（自建 dashboard）

> 未验证项：COROS 中国区账号与 MCP 的具体差异（官方指南提到按区域提供链接，但 CN 区可用性未实测）；写入 API 的最终形态（OAuth 授权范围、是否开放给第三方应用而不仅是 AI 助手）。这两点应在启动 B 组前实测确认。

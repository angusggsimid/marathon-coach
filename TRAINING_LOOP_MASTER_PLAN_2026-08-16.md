# 训练闭环总纲：三时程控制模型 + 分级自治 + 路线图

- **日期**：2026-08-16
- **状态**：已确认（用户拍板全部接受）
- **关联**：`ADR_PWA_MCP_ARCHITECTURE_2026-08-15.md`（形态决策）、`COROS_MCP_DEEP_RESEARCH_2026-08-15.md`（数据基础）

---

## 0. 一句话总纲

**COROS 实测数据通过 insights 转化为训练引擎的输入；不同信号作用在不同时间尺度（校准/周期/就绪三层）；自动化程度按影响面分级（L1/L2/L3）；普通用户与 COROS 用户共用同一引擎，靠参数解析链优雅降级。**

## 1. 三时程控制模型（信号 → 决策对象）

| 层 | 信号 | 时程 | 决定什么 | 科学依据 |
|---|---|---|---|---|
| **校准层** | LT、LTHR、VO2max | 长期（周-月） | 全部配速区间、VDOT 基线 | Daniels VDOT：体能测试校准强度 |
| **周期层** | 有氧解耦、EF 趋势 | 中期（周-月） | 阶段转换时机、跑量爬升斜率 | Seiler：有氧就绪才上强度；EF 是纵向适应标记 |
| **就绪层** | HRV、静息心率、睡眠负债、COROS 恢复度、负荷比 | 短期（日-周） | 下一次课的强度 + 下一周的量 | HRV 指导训练（Kiviniemi/Vesterinen）；ACWR 周度风险（Gabbett） |

**引擎能力缺口**：现引擎只有周级调节（factor 缩放距离）。就绪层生效需新增"单课就绪门"（任务 3）；周期层生效需新增"阶段就绪门"（任务 4）。

## 2. 分级自治（Override 问题的最终答案）

| 级别 | 决策 | 方式 | 理由 |
|---|---|---|---|
| **L1** | 下一次课强度微调 | 自动执行 + 通知 + 用户否决权 | 小、可逆、高频；HRV 指导训练研究即自动执行 |
| **L2** | 下周 factor（0.90/1.00/1.05） | 客观裁决预填 + 一键确认 | 周结构值得知情，一键即可 |
| **L3** | LT/LTHR 校准 | 显式确认 + 差异展示 | 影响面大（全部配速改变） |

**两条铁律**：
1. **RPE 保留但降为否决通道**：主观感受捕捉生理数据测不到的信息（Saw 2016：主观指标对训练负荷变化更敏感）。COROS 用户的打卡从"主驱动"变"否决权"。
2. **冲突取保守**：主观与客观矛盾时取更保守者（任一说累就当累）。伤病预防原则。

**双用户架构**：引擎只有一条路径——参数解析链 `COROS 实测 ?? 简单输入推算 ?? 人群默认`（`resolveLTPaceSec` 已是此模式的现成样板）。普通用户零感知，COROS 用户自动命中高优先级。

## 3. 同步频率（PWA 集成后的计时器）

| 数据层 | 频率 | 消费时机 |
|---|---|---|
| 就绪层（HRV/恢复/睡眠/静息心率） | 每 1-3 天 | 强度课前（就绪门） |
| 负荷（活动列表） | 每 3 天 | 周切换（factor） |
| 校准层（体能评估） | 每 2-4 周 | 检测到达（校准） |

## 4. 引擎输入全清单（精确到字段）

### A 类：自动写入（设备实测，高置信）
| 字段 | 来源 | 引擎效果 |
|---|---|---|
| `profile.ltPace` | COROS 实测阈值 | resolveLTPaceSec 第一优先级 → 6 配速区间 + 全部目标配速重算 |
| `profile.lthr` | LT 配速分圈心率估算（≥3 圈） | 心率区间个人化（替代默认 167） |

### B 类：建议、人决策
| 字段 | 输出 | 不自动的原因 |
|---|---|---|
| `profile.intensity` | Seiler 缺口 → 升档/执行建议 | 周跑量 ×1.25 + 强度课数，属偏好 |
| `profile.pb*` | 预测 vs PB 对照 | PB 应来自真实比赛 |
| `profile.goalTime` | 目标 vs 预测差距 | 目标设定是人的决策 |

### C 类：客观裁决（L2 注入，任务 2）
六信号 → factor ∈ {0.90, 1.00, 1.05}：

| 信号 | 风险 | 积极 |
|---|---|---|
| 负荷比 7 天均值 | >1.5 | 0.8-1.3 |
| EF 趋势 | <-1%/周 | >+1%/周 |
| 睡眠负债 14 天 | ≥300 分钟 | <120 分钟 |
| HRV 7 天 vs 基线 | ≥60% 天低于 | ≥60% 天不低于 |
| 静息心率 7 天 vs 基线 | >+3 bpm | 不超基线 |
| COROS 恢复度 | <40% | ≥70% |

**风险 ≥2 → 0.90；风险 0 且积极 ≥2 → 1.05；其余 → 1.00**

### D 类：永不进引擎
步频/步幅/触地时间（跑姿反馈，非剂量输入）、height/weight、raceDate/raceType、longRunDay、myRaces/vacations。

## 5. 路线图

| 阶段 | 内容 | 引擎改动 |
|---|---|---|
| **任务 1** | insights 教练处方 + 备份桥（`marathon-coach-rx v1` 契约） | 零 |
| **任务 2** | insights 集成进 PWA + MCP 同步计时器 + L2 落地（factor 注入口） | 小 |
| **任务 3** | L1 落地（单课就绪门）+ L3 流程（校准自动化） | 中 |
| **任务 4** | 周期层：解耦就绪门 + EF 调节进展斜率 + VO2max×VDOT 交叉校验 | 中 |

排序逻辑：先复用现有机制（factor 已有），后建新机制（课级门、阶段门）。每档对普通用户无损降级。

## 6. 任务 1 定稿方案（本次实施）

- **交互契约**：insights 产出引擎语言的处方（Recommendation[] + 校准备份 + 处方 JSON）
- **备份桥**：主 App 导出备份 → insights 导入对比 → 打补丁（仅 `profile.ltPace` + `profile.lthr`）→ 用户恢复 → 重新生成计划。主 App 零改动
- **六信号裁决**在任务 1 以契约形式呈现（任务 2 注入）
- **强验证**：校准备备必须通过主 App 真实 `parseBackupJson`
- 交付物：`insights/src/lib/coach.ts`、`CoachSection.tsx`（页面顶部，结论先行）、selftest ~18 例、CONTEXT 记录
